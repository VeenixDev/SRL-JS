EMAIL()

An example how you could implement an simple email check.

@author Paul Schmeing
@version 1.0
---
DEFINE "localpart" FOR [
	FROM (!letter !number "._%+-") MANY1
]
DEFINE "domain" FOR [
	FROM (!letter !number "._%+-") MANY1
]
DEFINE "tld" FOR [
	FROM (!letter) BETWEEN($2, $4)
]

SUBRUTINE ("localpart")
LITERAL ("@")
SUBRUTINE ("domain")
LITERAL (".")
SUBRUTINE ("tld")