EMAIL()

An example how you could implement an simple email check.

@author Paul Schmeing
@version 1.0
---
; "localpart" FOR [ FROM ("A-Za-z0-9._%+-") MORE THAN 0 ] "@" "domain" FOR [ FROM ("A-Za-z0-9._%+-") MORE THAN 0 ] "." "tld" FOR [ FROM ("A-Z") BETWEEN 2 AND 4 ]


"localpart" FOR [
	FROM ("A-Za-z0-9._%+-") MORE THAN 0
]
LITERAL "@"
"domain" FOR [
	FROM ("A-Za-z0-9._%+-") MORE THAN 0
]
LITERAL "."
"tld" FOR [
	FROM ("A-Z") BETWEEN 2 AND 4
]
